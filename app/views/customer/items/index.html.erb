<div class="container">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <h2 class="head-line title_h2 text-center">商品一覧
        <span>
			    (全<%= @items.total_count %>件)
		    </span>
      </h2>
    </div>
  </div>
  <div class="row">
    <div class="d-flex flex-row flex-wrap">
      <% @items.each do |item| %>

      <% end %>
    </div>
  </div>
  <%= paginate @items %>
</div>