<header>
  <nav>
    <% if admin_signed_in? %>
      <%= link_to "NaganoCake", admin_top_path %>
    <% else %>
      <%= link_to "NaganoCake", root_path %>
    <% end %>
  </nav>
  <nav>
    <ul>
      <% if customer_sign_in? %>

      <% end %>
    </ul>
  </nav>
</header>