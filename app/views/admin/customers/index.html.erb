<h2>会員一覧</h2>
<div>
  <div>
    <table>
      <thead>
        <tr>
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>

      <tbody>
        <%= @customers.each do |customer| %>
          <tr>
            <td>
              <%= customer.id %>
            </td>
            <td>
              <!--フルネーム(姓名)で表示させたいが実装方法が不明のため後ほど実装する-->
              <!--< %= customer.last_name + customer.first_name %>-->
            </td>
          </tr>
      </tbody>
    </table>
  </div>
</div>