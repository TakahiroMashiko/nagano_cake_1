<div class="container">
  <div class="row">
    <div class="col-xs-3">
      <h2 class="head-line title_h2 text-center bg-light">商品詳細</h2>
    </div>
  </div>
</div>
<div>
  <div>
    <div>
      <div>
        <%= attachment_image_tag(@item, :image, size: "300x230", fallback: "no_image.jpg") %>
      </div>
    </div>
    <div>
      <div>
        <p>商品名 <%= @item.name %></p><br>
      </div>
      <div>tax_price
        <p>商品説明 <%= @item.introduction %></p><br>
      </div>
      <!--任意実装-->
      <!--<div>-->
      <!--  <p>ジャンル < %= @item.genre.name %></p><br>-->
      <!--</div>-->
      <div>
        <!--<p>税込価格 < %= tax_price(@item.price).to_s(:delimited) %>	(<%= @item.price.to_s(:delimited) %>) 円-->
				<br>(税抜価格)
				</p><br>
      </div>
      <!--任意実装-->
			<!--<div>-->
      <!--<p>販売ステータス</p>-->
      <!--</div>-->
      <div>
        <%= link_to "編集する", edit_admin_item_path(@item) %>
      </div>
    </div>
  </div>
</div>