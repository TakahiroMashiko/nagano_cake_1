<div class="container">
  <div class="row">
    <div class="col-xs-offset-1 col-xs-11">
      <h2 class="head-line title_h2 text-left bg-light">注文履歴一覧</h2>
     </div>
  </div>
</div>
<br>
<div class="container">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <table class="table table-hover table-bordered table-condensed bg-light">
         <thead>
          <tr class="info">
            <th class="col-xs-3">購入日時</th>
            <th>購入者</th>
            <th class="col-xs-1">注文個数</th>
            <!--任意実装-->
            <!--<th>注文ステータス</th>-->
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
            <tr>
              <td>
                <%= link_to admin_order_path(:order) do %>
                  <% order.created_at %>
                <% end %>
              </td>
              <td class="text-left">
                <%= full_name(order.customer) %>
              </td>
              <td class="text-right">
                <%= order.order_details.count %>
              </td>
              <!--任意実装-->
              <!--<td>-->
              <!--  < %= order.order_status %>-->
              <!--</td>-->
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div><%= paginate @orders %></div>
  </div>
</div>